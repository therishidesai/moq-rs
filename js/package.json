{
	"name": "moq",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"workspaces": [
		"moq",
		"moq-clock", 
		"moq-token",
		"hang",
		"hang-demo",
		"signals"
	],
	"scripts": {
		"check": "bun run --filter='*' check",
		"fix": "bun run --filter='*' fix"
	},
	"devDependencies": {
		"concurrently": "^9.2.1"
	}
}
